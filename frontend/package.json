{
  "name": "eventpro-frontend",
  "version": "1.0.0",
  "private": true,
  "description": "EventPro web app + Capacitor Android wrapper.",
  "scripts": {
    "cap:add:android": "cap add android",
    "cap:sync": "cap sync android"
  },
  "dependencies": {
    "@capacitor/android": "^6.1.2",
    "@capacitor/core": "^6.1.2"
  },
  "devDependencies": {
    "@capacitor/cli": "^6.1.2"
  }
}
